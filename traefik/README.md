# Traefik

1.  `$ mkdir -p ~/cloud-data/traefik/letsencrypt ~/cloud-data/traefik/certs`
1.  `$ cp .env.tmpl .env`
1.  Input appropriate values
1.  `$ docker-compose up -d`
1.  Open Traefik dashboard at `https://traefik.odroid.noizwaves.cloud`
