# Password for the 'elastic' user (`openssl rand -base64 12`)
ELASTIC_PASSWORD=""

# Password for the 'kibana_system' user (`openssl rand -base64 12`)
KIBANA_PASSWORD=""

# Version of Elastic products
STACK_VERSION=8.7.1

# Set the cluster name
CLUSTER_NAME=odroid

LICENSE=basic

# Increase or decrease based on the available host memory (in bytes)
ES_MEM_LIMIT=2147483648
KB_MEM_LIMIT=2147483648
LS_MEM_LIMIT=2147483648

# SAMPLE Predefined Key only to be used in POC environments (or `openssl rand -hex 32`)
ENCRYPTION_KEY=""
